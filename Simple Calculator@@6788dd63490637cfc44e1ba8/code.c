#include <stdio.h>
int main(){
    int a,b,sum,diff,prod,div;
    scanf("%d%d", &a, &b);
    if(a,b,+){
        sum=a+b;
        printf("%d", sum);
    }
    else if(a,b,-){
        diff=a-b;
        printf("%d", diff);
    }
    else if(a,b,*){
        prod=a*b;
        printf("%d", prod);
    }
    else if(a,b,/){
        div=a/b;
        printf("%d", div);
    }
    else{
        printf("Error");
    }
    return 0;
}